<!--
Autor: AGUSTÍN IGLESIAS MOYA
Fecha: 22/04/2017

Controlador que interactua con la vista: NivelEducativoController

Vista que muestra:
    En una tabla la lista de planes de estudios recibidas del controlador, con un checkbox en cada fila de la tabla para seleccionar el elemento a borrar
    Botón para añadir un nuevo plan de estudio
    Botón para eliminar los planes de estudio seleccionados en los checkbox

-->
<!--Colección de Niveles educativos a mostrar-->
@model IEnumerable<TFG.Models.NivelEducativo>

@{
    ViewData["Title"] = "Niveles Educativo";
}


<hr />
<!--Formulario que será enviado por POST al pulsar en el botón borrarNivelEducativo-->
<form asp-action="BorrarNivelEducativo" role="form">
    <div class="top-buffer"></div>
    <div class="panel-body">                    
        <div class="panel panel-default">
            <div class="panel-heading">
                @ViewData["Title"]
                <div class="pull-right">
                    <!--Botón para añadir un nuevo plan de estudio-->
                    <a id="crearEspecialidad" asp-action="CrearEditarNivelEducativo" class="btn btn-default btn-sm">
                        <i class="glyphicon glyphicon-plus"></i>Nuevo Nivel Educativo
                    </a>
                    <!--Javascript para confirmar que se desean borrar los datos-->
                    <span onclick="return confirm('Se perderán los datos. ¿Desea continuar?')">
                        <button data-dismiss="modal" id="borrarNivelEducativo" class="btn btn-danger btn-sm" type="submit">
                            <i class="glyphicon glyphicon-trash"></i> Borrar(Seleccionados)
                        </button>
                    </span>
                </div>
            </div>
            <div class="alert-danger">
                @Html.ValidationSummary(false)
            </div>
            <div class="panel-body">
                <div class="top-buffer"></div>
                <table id="example" class="table table-striped table-bordered">
                    <thead><!--Encabezado de la tabla donde se muestran los datos del nivel educativo-->
                        <tr>
                            <th></th>                            
                            <th>Nivel Educativo(@Html.DisplayNameFor(x => x.Descripcion))</th>
                        </tr>
                    </thead>
                    <tbody><!--Datos del nivel educativo mostrados en la tabla-->
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>                              
                                    <input name="selBorrar" type="checkbox" value="@item.Id" />                            
                                </td>
                                <td>
                                    <a id="editarNivelEducativo" asp-action="CrearEditarNivelEducativo" asp-route-id="@item.Id">@item.Descripcion</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            </div>
        </div>
</form>
